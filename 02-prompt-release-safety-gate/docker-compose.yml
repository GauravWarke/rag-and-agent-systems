services:
  safety-gate:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped

  gate-runner:
    build: .
    profiles: ["gate"]
    env_file:
      - .env
    entrypoint: ["python", "gate.py"]
    volumes:
      - ./reports:/app/reports
