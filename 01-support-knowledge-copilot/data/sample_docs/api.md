# Authentication
Send your API key in the Authorization header as a Bearer token. Keys are created in the dashboard.

# Pagination
List endpoints return 50 items per page. Use the `cursor` query parameter to fetch the next page.
